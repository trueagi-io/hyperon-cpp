FIND_PACKAGE(CxxTest REQUIRED)

MACRO(ADD_CXXTEST)
	CXXTEST_ADD_TEST(${ARGV0} ${ARGV0}.cpp ${CMAKE_CURRENT_SOURCE_DIR}/${ARGV0}.h)
	TARGET_LINK_LIBRARIES(${ARGV0} hyperon hyperon_common)
ENDMACRO()

ADD_SUBDIRECTORY(hyperon)
